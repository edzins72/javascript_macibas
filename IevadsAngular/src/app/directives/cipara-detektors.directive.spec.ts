import { CiparaDetektorsDirective } from './cipara-detektors.directive';

describe('CiparaDetektorsDirective', () => {
  it('should create an instance', () => {
    const directive = new CiparaDetektorsDirective();
    expect(directive).toBeTruthy();
  });
});
